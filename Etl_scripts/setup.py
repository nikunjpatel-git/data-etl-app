# Setup file for helper_utils whl
# command line command to create whl: python setup.py bdist_wheel
from setuptools import setup, find_packages
setup(
    name='helper_utils',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[],
)


